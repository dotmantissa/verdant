// Wallet state lives in WalletProvider (React context).
// This file re-exports the hook so existing imports don't need to change.
export { useWallet } from "@/components/WalletProvider";
export type { } from "@/components/WalletProvider";
